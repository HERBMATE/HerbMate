<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Warna saat chip dipilih -->
    <item android:state_checked="true" android:drawable="@color/selected_chip_color" />
    <!-- Warna saat chip tidak dipilih -->
    <item android:drawable="@color/unselected_chip_color" />
</selector>
